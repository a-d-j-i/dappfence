---
export const partial = true;
---

<ul>
    <li><strong>secp256k1</strong> — manifest signing with Ethereum-compatible key recovery</li>
    <li><strong>SHA-256</strong> — file integrity hashing in SRI format (<code>sha256-…</code>)</li>
    <li><strong>Service Worker</strong> — intercepts every fetch before it reaches the page</li>
    <li>
        <strong>IndexedDB</strong> — persists verified hashes and security events across sessions
    </li>
    <li>
        <strong>@dappfence/astro</strong> — copies runtime, injects script tag, signs manifest at build
        time
    </li>
</ul>
